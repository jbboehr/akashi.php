# Planning

The repository contains the shared project scaffold, a minimal executable entry point, the validated runtime
dependencies, and the initial immutable document and example model. Markdown discovery, parsing, execution, and
verification APIs remain to be implemented.

The detailed implementation handoff is maintained separately from the public mdBook sources.
